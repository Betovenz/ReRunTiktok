# SPRR01 — Rerun Studio + AdminWEB
## สรุป Flow · Step · ปุ่มทั้งหมด · และข้อเสนอ UI ใหม่

> เอกสารนี้เขียนเพื่อส่งต่อให้ Claude Design ใช้ออกแบบ UI ใหม่
> อ้างอิงจากโค้ดจริงใน `05-updated-source-from-asar` (Rerun Studio 0.7.6) และ `06-adminweb-backoffice`

---

## 0. TL;DR

| หัวข้อ | ปัจจุบัน | ที่เสนอใหม่ |
| --- | --- | --- |
| จำนวนขั้นตอนก่อนไลฟ์ได้ | **6 ขั้น** (Wizard 1→6) | **3 ขั้น** (เตรียม → ตกแต่ง → ยิงไลฟ์) |
| จำนวนปุ่มในแอปหลัก | **~118 ปุ่ม/คอนโทรล** | คงฟีเจอร์เดิม แต่ซ่อนใต้ "โหมดขั้นสูง" เหลือ ~45 ปุ่มที่เห็นตอนแรก |
| ปัญหาใหญ่สุด | ขั้นที่ 4 ยัดของ 3 ระบบไว้ด้วยกัน (ปลายทาง + ปักสินค้า + แชท AI) | แยกเป็น "ตั้งค่าก่อนไลฟ์" กับ "แผงควบคุมตอนไลฟ์" |
| จุดที่ผู้ใช้หลงบ่อย | ปุ่มปักสินค้า/คูปองกดไม่ได้ตอนยังไม่ไลฟ์ แต่ยังโชว์ในขั้นที่ 4 | ย้ายไปหน้า Live Control ทั้งหมด กดได้จริงเสมอ |
| แอดมิน 2 ที่ | AdminWEB (ลูกค้า/แพ็กเกจ) + โหมดแอดมินในแอป (License Key) | รวมเป็น AdminWEB ที่เดียว |

---

## 1. ระบบนี้ประกอบด้วยอะไร

```
┌─────────────────────────────────────────────────────────────┐
│  AdminWEB (localhost:4140)  ── Node + MongoDB Atlas         │
│  เจ้าของระบบใช้: สร้างแพ็กเกจ / ลูกค้า / ประกาศ             │
└───────────────┬─────────────────────────────────────────────┘
                │ รหัสลูกค้า + Username + Password
                ▼
┌─────────────────────────────────────────────────────────────┐
│  Rerun Studio 0.7.6 (Electron + React 18)                   │
│  ลูกค้าใช้: เอาคลิปวนซ้ำ → ยิงไลฟ์ TikTok อัตโนมัติ         │
│  ├ FFmpeg pipeline  → RTMP                                  │
│  ├ TikTok session (persistent Electron profile)             │
│  ├ Pin สินค้า/คูปองอัตโนมัติ (คุมหน้า LIVE console)         │
│  ├ ตอบแชทอัตโนมัติ (Keyword + AI Gemini/Claude)             │
│  └ แจ้งเตือน LINE + auto-update (electron-updater)          │
└─────────────────────────────────────────────────────────────┘
```

**ข้อมูลเทคนิคที่ต้องรู้เวลาออกแบบ**
- วิดีโอออกที่ **1080 × 1920 (9:16)** เสมอ · 30 fps · AAC 128 kbps
- รองรับ **หลายบัญชีพร้อมกัน** บนเครื่องเดียว (จำนวนขึ้นกับสเปกเครื่อง + แพ็กเกจ)
- ทุกบัญชีมี wizard/สถานะแยกกันอิสระ
- แอปเป็น **ภาษาไทยทั้งหมด** ผู้ใช้คือแม่ค้าไลฟ์ ไม่ใช่สาย IT

---

## 2. Flow ปัจจุบัน (As-is)

### 2.1 ฝั่งเจ้าของระบบ (AdminWEB)

```
เปิด START-ADMIN.bat
   └→ เบราว์เซอร์เด้ง localhost:4140
      └→ Login (Username / Password)
         └→ [ภาพรวม] ดูสถิติ
            ├→ [แพ็กเกจ] สร้างแพ็กเกจก่อน (รหัส / ชื่อ / ราคา / จำนวนช่อง / จำนวนวัน)
            ├→ [ลูกค้า]  สร้างลูกค้า (รหัสลูกค้า / ชื่อ / Username / Password / แพ็กเกจ / วันหมดอายุ)
            └→ [ประกาศ]  ตั้งข้อความที่จะเด้งในแอปลูกค้า
         └→ ส่ง "รหัสลูกค้า + Username + Password" ให้ลูกค้า
```

### 2.2 ฝั่งลูกค้า (Rerun Studio) — ปัจจุบัน 6 ขั้น

```
เปิดแอป
 └→ หน้า License Login: รหัสลูกค้า / Username / Password
    └→ [แดชบอร์ด] ยินดีต้อนรับ + สรุปยอด + ทางลัด
       └→ กด "เริ่มไลฟ์ใหม่" → ไปหน้า [บัญชีไลฟ์]
          │
          ├ เลือก/เพิ่มบัญชี (chip ด้านบน) — แต่ละบัญชีมี wizard ของตัวเอง
          │
          ├─ ขั้น 1  เข้าสู่ระบบ        → login TikTok ในหน้าต่างที่แอปเปิดให้
          ├─ ขั้น 2  เลือกวิดีโอ        → เลือก MP4/WebM หลายคลิป + จัดลำดับ + สุ่ม
          ├─ ขั้น 3  โอเวอร์เลย์        → รูป / นาฬิกา / ข้อความ + ปรับกล้อง
          ├─ ขั้น 4  เชื่อม TikTok      → ปลายทาง + คุณภาพ + ปักสินค้า + แชท AI  ← หนักที่สุด
          ├─ ขั้น 5  พร้อมไลฟ์          → เช็กลิสต์ 5 ข้อ + ปุ่ม "เริ่มไลฟ์ตอนนี้"
          └─ ขั้น 6  กำลังไลฟ์          → สถานะ + แชทสด + ปักสินค้า + หยุดไลฟ์
```

### 2.3 Flow ย่อยที่ผู้ใช้ทำบ่อยหลังไลฟ์จบ

```
[สรุปไลฟ์ย้อนหลัง]  ดูระยะเวลา/สถานะการจบ (เก็บล่าสุด 200 รายการ)
[แดชบอร์ด]         นำเข้ายอดขาย CSV → map คอลัมน์ → บันทึกออร์เดอร์ → เห็นกราฟรายได้
[คลังวิดีโอ]        เก็บคลิปที่ใช้ซ้ำบ่อย
[ระบบ & ตั้งค่า]    เช็กอัปเดต / เบนช์มาร์กเครื่อง / ตั้ง LINE แจ้งเตือน
```

---

## 3. Step ปัจจุบันแบบละเอียด (Wizard 6 ขั้น)

### ขั้น 1 — "เข้าสู่ระบบ"
- หัวข้อ: *เข้าสู่ระบบ TikTok ที่จะใช้ไลฟ์* / เมื่อสำเร็จ: *TikTok พร้อมใช้งาน*
- ช่องกรอก: **ชื่อบัญชีในแอป** (เช่น "ร้านหลัก")
- ระบบเปิดหน้าต่าง TikTok ให้ล็อกอินเอง → หน้าต่างปิดเอง → จำ session แบบเข้ารหัสในเครื่อง
- ข้อความความปลอดภัย: *ระบบเก็บ browser session ไว้เฉพาะเครื่องนี้ และไม่อ่านหรือส่งออกรหัสผ่านของคุณ*

### ขั้น 2 — "เลือกวิดีโอ"
- หัวข้อ: *เลือกวิดีโอที่จะเล่นซ้ำ*
- รองรับ MP4 / WebM · pipeline ปรับเป็น 1080×1920 ก่อนส่ง
- คิวคลิป: ลากลำดับด้วยลูกศรขึ้น/ลง
- สวิตช์ **สุ่มลำดับเล่น** (ต้องมี ≥ 2 คลิป) — *คลิปใหม่ทุกรอบ กันตรวจจับว่าเล่นซ้ำแพทเทิร์นเดิม*
- เตือนเมื่อ *คลิปสเปคไม่ตรงกัน — ระบบจะปรับให้ตรงกันอัตโนมัติตอนกดเริ่มไลฟ์*

### ขั้น 3 — "โอเวอร์เลย์"
- หัวข้อ: *จัดการ Overlay* · พรีวิว 9:16 ลากวางได้
- Layer 3 ชนิด: **รูป (สูงสุด 12)** · **นาฬิกา (สูงสุด 6)** · **ข้อความ**
- คุมต่อ layer: ขนาด (64–900px) · ความโปร่งใส · เอฟเฟกต์ (ไม่มี / กะพริบ / จางเข้า-ออก / ลอยขึ้น-ลง) · ลำดับ · ลบ
- ข้อความเพิ่ม: โหมด **นิ่ง / วิ่ง (marquee)** · ฟอนต์ (เรียบง่าย / โมเดิร์น / โมโนสเปซ / คลาสสิก / โปสเตอร์ / พิกเซลเกม / เทอร์มินัล / เทคโน / LCD 14 ส่วน) · สี · ขนาด · ตัวจัดวาง · สไตล์ขอบ (กล่องดำ / กล่องส้ม / ขอบตัด / เงา / พื้นใส / มีเชิง / ทึบหนัก / หนาเด่น)
- นาฬิกาเพิ่ม: รูปแบบเวลา (24 ชม. HH:MM / HH:MM:SS / 12 ชม. AM-PM / วันที่ไทย / วัน+เวลา)
- **ปรับกล้อง**: ซูม 1.0–3.0 · ลากเลื่อนตำแหน่งบนพรีวิว · พลิกกล้อง (สะท้อน) · รีเซ็ตกล้อง
- ตัวช่วย: เส้น **พื้นที่ปลอดภัย**

### ขั้น 4 — "เชื่อม TikTok" ← ขั้นที่หนักเกินไป
รวม **4 ระบบ** ไว้ในหน้าเดียว:

1. **ปลายทางสตรีม**
   - โหมด **TikTok อัตโนมัติ** — ระบบสร้างห้อง LIVE + รับ RTMP ให้เอง (มีลำดับ 1 สร้างห้อง Streamer Desktop → 2 …)
   - โหมด **Manual RTMP** — กรอก Server URL + Stream key เอง (มีปุ่มตา แสดง/ซ่อน)
   - ปุ่ม **เปิด LIVE Manager** · ช่อง **ชื่อ LIVE**
2. **คุณภาพการส่ง** — bitrate (แนะนำ 6 Mbps) + เลือกตัวเข้ารหัส (ฮาร์ดแวร์/CPU)
3. **ปักสินค้าอัตโนมัติ** — สวิตช์เปิด · ดึงสินค้าจากไลฟ์ · ทุกกี่นาที · จัดลำดับสินค้า · ข้าม/ใส่กลับรายตัว · ปักเลย · ใส่คูปองในรอบการวน · ปักคูปองเลย
   ⚠️ ปุ่ม "ปักเลย" และ "ปักคูปองเลย" **กดไม่ได้ถ้ายังไม่ไลฟ์** (*ต้องกำลังไลฟ์อยู่ถึงจะปักได้*) — แต่ยังโชว์อยู่ในขั้นตอนตั้งค่า
4. **แชทสด & การตอบกลับ** — สวิตช์เปิด · @username โฮสต์ · กฎ คีย์เวิร์ด→คำตอบ (ฟรี ตอบทันที) · AI (Gemini / Claude) + API key + โมเดล + บริบทร้าน + ให้ AI รู้จักสินค้าในไลฟ์

### ขั้น 5 — "พร้อมไลฟ์"
- **สรุปการตั้งค่า** 5 บรรทัด: บัญชี · วิดีโอ · Overlay (จำนวน layer) · ปลายทาง · Bitrate
- ปุ่มใหญ่ **"เริ่มไลฟ์ตอนนี้"** → *ระบบจะสร้างห้อง LIVE จริงเฉพาะเมื่อคุณกดปุ่มนี้เท่านั้น*

### ขั้น 6 — "กำลังไลฟ์"
- สถานะ pipeline · บิตเรตที่ส่งจริง · fps · สุขภาพ
- แผงแชทสด (คอมเมนต์เข้า/คำตอบที่ระบบส่ง)
- ปักสินค้า/คูปองแบบกดเดี๋ยวนี้ · ยอดสด (GMV) จากหน้า TikTok
- ปุ่ม **อัปเดตให้ไลฟ์นี้** (แก้ overlay/คลิป/กล้อง แล้วมีผลทันที ภาพสะดุด 1–2 วิ)
- ปุ่ม **หยุดไลฟ์**
- พรีวิวหยุดไว้ระหว่างไลฟ์เพื่อลดภาระเครื่อง

---

## 4. สรุปปุ่มทั้งหมด

### 4.1 Rerun Studio — โครงหลัก (Shell)

| ที่อยู่ | ปุ่ม / คอนโทรล | ทำอะไร |
| --- | --- | --- |
| Sidebar | แดชบอร์ด | ไปหน้าแรก |
| Sidebar | บัญชีไลฟ์ | ไป wizard ตั้งค่า/ไลฟ์ |
| Sidebar | คลังวิดีโอ | คลิปที่เก็บไว้ใช้ซ้ำ |
| Sidebar | สรุปไลฟ์ย้อนหลัง | ประวัติไลฟ์ |
| Sidebar | ระบบ & ตั้งค่า | ระบบ / LINE |
| Sidebar | โหมดแอดมิน | จัดการ License Key |
| Sidebar ล่าง | จุดสถานะ FFmpeg (online/offline) | บอกว่าเครื่องพร้อมไหม |
| Sidebar ล่าง | ออกจากระบบ | ออกจาก License |
| Sidebar ล่าง | ป้ายเวอร์ชัน `Rerun Studio v0.7.6` | — |
| Topbar | **อัปเดตพร้อมติดตั้ง · รีสตาร์ต** (pill) | ติดตั้งอัปเดตทันที |
| Topbar | pill "กำลังดาวน์โหลด %" | สถานะอย่างเดียว |

### 4.2 หน้า Login (License)

| ปุ่ม / ช่อง | หมายเหตุ |
| --- | --- |
| รหัสลูกค้า (input) | เช่น BMK-0001 |
| Username (input) | |
| Password (input) + ปุ่มตา แสดง/ซ่อนรหัสผ่าน | |
| **เข้าสู่ระบบ** (primary) | |
| ดูคลิปสอนใช้งาน (เปิดใน YouTube) | เปิดลิงก์นอก |

### 4.3 แดชบอร์ด

| ปุ่ม | ทำอะไร |
| --- | --- |
| **เริ่มไลฟ์ใหม่** (primary large) | ไปหน้าบัญชีไลฟ์ |
| คลังวิดีโอ | ไปคลัง |
| นำเข้ายอดขาย (CSV) | เลือกไฟล์ → พรีวิว |
| — ยกเลิก / **บันทึก N ออร์เดอร์** | ยืนยันการนำเข้า (map คอลัมน์: ยอดเงิน จำเป็น / เลขคำสั่งซื้อ / วันเวลา) |
| — ลบชุดข้อมูลนี้ (ต่อไฟล์) | ลบ batch |
| ดูทั้งหมด → | ไปประวัติ |
| ทางลัด: เริ่มไลฟ์ · คลังวิดีโอ · ประวัติไลฟ์ · ตั้งค่า | 4 การ์ดใหญ่ |

การ์ดข้อมูล: ยอดขายรวม · ยอดขายรายวัน (14 วัน) · ชั่วโมงไลฟ์รายวัน · ผลงานรายช่อง · ไลฟ์ล่าสุด

### 4.4 หน้าบัญชีไลฟ์ (แถบบัญชี + Wizard)

| ปุ่ม | ทำอะไร |
| --- | --- |
| chip บัญชี (จุดสถานะ + ชื่อ + สถานะ) | สลับบัญชีที่กำลังตั้งค่า |
| ✕ ลบบัญชี | ลบ chip |
| + เพิ่มบัญชี | สร้างบัญชีใหม่ (wizard แยก) |
| stepper 1–6 | กดข้ามได้เฉพาะขั้นที่ผ่านแล้ว |
| **เริ่มตั้งค่าใหม่** | รีเซ็ตกลับขั้น 1 |
| ← ย้อนกลับ / ถัดไป → | เดินขั้นตอน |

**ขั้น 1**

| ปุ่ม |
| --- |
| ชื่อบัญชีในแอป (input) |
| **เข้าสู่ระบบด้วย TikTok** |
| เปิด TikTok |
| ออกจากระบบ (TikTok) |

**ขั้น 2**

| ปุ่ม |
| --- |
| Dropzone: เลือกวิดีโอจากเครื่อง / เพิ่มวิดีโออีกคลิป |
| ▲ เลื่อนขึ้น (เล่นก่อน) · ▼ เลื่อนลง (เล่นทีหลัง) · 🗑 เอาออกจากรายการ (ต่อคลิป) |
| Toggle: สุ่มลำดับเล่น |

**ขั้น 3**

| ปุ่ม |
| --- |
| + เพิ่มรูป (≤12) · + เพิ่มนาฬิกา (≤6) · + เพิ่มข้อความ |
| ต่อ layer: ▲ ▼ · 🗑 ลบรูป / ลบนาฬิกา / ลบข้อความ |
| Slider: ขนาด · ความโปร่งใส · ขนาดตัวอักษร |
| Select: เอฟเฟกต์ · ฟอนต์ · สี · ตัวจัดวาง · สไตล์ขอบ · รูปแบบเวลา |
| ข้อความ: นิ่ง / วิ่ง |
| กล้อง: slider ซูม · **พลิกกล้อง (สะท้อน)** · รีเซ็ตกล้อง · ลากบนพรีวิว |

**ขั้น 4**

| กลุ่ม | ปุ่ม |
| --- | --- |
| ปลายทาง | TikTok อัตโนมัติ / Manual RTMP (tab) · เปิด LIVE Manager · ชื่อ LIVE · Server URL · Stream key + 👁 แสดง/ซ่อน |
| คุณภาพ | bitrate slider · เลือกตัวเข้ารหัส |
| ปักสินค้า | Toggle เปิดปักอัตโนมัติ · **ดึงสินค้าจากไลฟ์** · ทุกกี่นาที (input) · chip สินค้า เปิด/ข้าม · **ปักเลย** (ต้องกำลังไลฟ์) · ▲▼ ลำดับปัก · 🗑 ลบออกจากรายการ · + เพิ่มสินค้าใหม่ · Toggle ใส่คูปองในรอบการวน · **ปักคูปองเลย** (ต้องกำลังไลฟ์) |
| แชท | Toggle เปิด/ปิดตอบอัตโนมัติ · @username โฮสต์ · **+ เพิ่มกฎ** · Toggle กฎรายข้อ · 🗑 ลบกฎ · Toggle ตอบด้วย AI · เลือก Gemini / Claude · API key · โมเดล · บริบทร้าน · Toggle ให้ AI รู้จักสินค้าในไลฟ์ · ไปหน้าขอ API key |

**ขั้น 5**

| ปุ่ม |
| --- |
| **เริ่มไลฟ์ตอนนี้** (primary large) — ระหว่างรอขึ้น "กำลังเริ่ม pipeline…" |

**ขั้น 6**

| ปุ่ม |
| --- |
| **หยุดไลฟ์** (stop) |
| อัปเดตให้ไลฟ์นี้ |
| ปักเลย / ปักคูปองเลย / ดึงสินค้าจากไลฟ์ (ตอนนี้ใช้ได้จริง) |

### 4.5 คลังวิดีโอ / ประวัติ

| หน้า | ปุ่ม |
| --- | --- |
| คลังวิดีโอ | **เพิ่มวิดีโอ** · 🗑 ลบออกจากคลัง |
| สรุปไลฟ์ย้อนหลัง | ล้างประวัติ (+ การ์ดสรุป: จำนวนไลฟ์ / เวลาไลฟ์รวม / จบปกติ / จบด้วยข้อผิดพลาด) |

### 4.6 ระบบ & ตั้งค่า

| แท็บ | ปุ่ม |
| --- | --- |
| **ระบบ** | Toggle อัปเดตอัตโนมัติ · ตรวจหาอัปเดต · อัปเดตเลย · รีสตาร์ต |
| | สแกนหน้า LIVE console: **สแกนตอนนี้** · คัดลอกผลทั้งหมด |
| | เครื่องนี้ไลฟ์ไหวแค่ไหน: **เริ่มทดสอบ / ทดสอบใหม่** (bench ~10 วิ → บอกว่าไลฟ์พร้อมกันได้กี่บัญชี) |
| | แสดงผล: เวอร์ชัน · FFmpeg พร้อม/ไม่พบ · ตัวช่วยฮาร์ดแวร์ · รองรับพร้อมกันสูงสุด |
| **แจ้งเตือน LINE** | Toggle เปิด · Channel access token · ปลายทาง (User/Group ID) · Toggle แจ้งเมื่อเริ่มไลฟ์ / หยุดไลฟ์ / ไลฟ์มีปัญหา · **บันทึก** · ส่งข้อความทดสอบ · ส่งสถานะไลฟ์ตอนนี้ |

### 4.7 โหมดแอดมิน (ในแอป)

| ปุ่ม |
| --- |
| ADMIN TOKEN (input) → **ปลดล็อก** |
| ล็อกโหมดแอดมิน |
| ออกคีย์ให้ผู้ใช้ใหม่: username / ชื่อที่แสดง / รหัสผ่าน / อายุ (วัน) → **สร้าง License Key** → 📋 คัดลอก |
| ค้นหาผู้ใช้ → **ค้นหา** → 📋 คัดลอกคีย์ · **ถอนสิทธิ์** |
| คีย์ที่ออกไว้: รีเฟรช · 📋 · ถอนสิทธิ์ (รายแถว) |

### 4.8 AdminWEB (localhost:4140)

| ที่อยู่ | ปุ่ม |
| --- | --- |
| Login | Username · Password · **เข้าสู่หลังบ้าน** |
| Sidebar | ภาพรวม · ลูกค้า · แพ็กเกจ · ประกาศ (มี badge ตัวเลข) |
| Topbar | ช่องค้นหารวม · Toggle โหมดมืด · เปิดหน้าแอป · ออกจากระบบ |
| ภาพรวม | การ์ด: ลูกค้าทั้งหมด · ลูกค้าใช้งาน · ใกล้หมดอายุ · ช่องที่ขาย · มูลค่าแพ็กเกจ · แผง: แพ็กเกจขายดี / ผู้ใช้ใหม่ 7 วัน / สถานะลูกค้า / ใกล้หมดอายุ / ประกาศในแอป |
| ลูกค้า | ล้างฟอร์ม · **บันทึกลูกค้า** · แถว: แก้ / **ลบ** · Modal แก้ไข: ปิด · ยกเลิก · **บันทึกการแก้ไข** (มีเปลี่ยนรหัสผ่าน + ยืนยัน) |
| แพ็กเกจ | เพิ่มใหม่ · **บันทึกแพ็กเกจ** · แถว: แก้ / ลบ |
| ประกาศ | เพิ่มใหม่ · **บันทึกประกาศ** · แถว: แก้ / ลบ |

> **ข้อจำกัดที่ต้องบอกใน UI:** ช่องที่ลูกค้าใช้จริงจะขึ้น 0 เสมอ เพราะบัญชี TikTok เก็บใน `data/state.json` บนเครื่องลูกค้า ไม่ได้อยู่บน MongoDB

### 4.9 นับรวม

| ส่วน | จำนวนปุ่ม/คอนโทรลโดยประมาณ |
| --- | --- |
| Shell + Login | 13 |
| แดชบอร์ด | 11 |
| Wizard ขั้น 1–3 | 24 |
| Wizard ขั้น 4 | 34 ← หนาแน่นที่สุด |
| Wizard ขั้น 5–6 | 7 |
| คลัง + ประวัติ | 4 |
| ระบบ & ตั้งค่า | 14 |
| โหมดแอดมิน | 11 |
| **รวม Rerun Studio** | **~118** |
| **AdminWEB** | **~24** |

---

## 5. ปัญหา UX ที่พบ (เรียงตามความรุนแรง)

| # | ปัญหา | ทำไมถึงเจ็บ |
| --- | --- | --- |
| 1 | **ขั้น 4 ยัด 4 ระบบไว้ด้วยกัน** (ปลายทาง / คุณภาพ / ปักสินค้า / แชท AI) มี 34 คอนโทรล | ผู้ใช้ใหม่หยุดตรงนี้ เพราะไม่รู้ว่าอะไร "จำเป็น" อะไร "ทำทีหลังได้" |
| 2 | **ปุ่มที่กดไม่ได้ อยู่ผิดที่** — "ปักเลย" / "ปักคูปองเลย" อยู่ในขั้นตั้งค่า แต่ต้องกำลังไลฟ์ถึงจะกดได้ | ผู้ใช้กดแล้วไม่เกิดอะไร → คิดว่าแอปเสีย |
| 3 | **6 ขั้นทั้งที่จำเป็นจริงแค่ 3** — Overlay กับ แชท AI เป็นของเสริม แต่ถูกวางขวางทาง | ยืดเวลาจาก "อยากไลฟ์" ถึง "ไลฟ์ได้" โดยไม่จำเป็น |
| 4 | **แอดมินอยู่ 2 ที่** — License Key ในแอป, ลูกค้า/แพ็กเกจใน AdminWEB | เจ้าของระบบต้องจำว่าเรื่องไหนอยู่ที่ไหน |
| 5 | **ไม่มีการบันทึกเป็น Preset** | ตั้งค่า overlay/สินค้า/แชทใหม่ทุกครั้งที่เริ่มไลฟ์รอบใหม่ |
| 6 | **ตอนไลฟ์อยู่ ยังต้องเดินใน stepper เดิม** | ขณะไลฟ์คือช่วงที่กดดันที่สุด แต่ UI ยังเป็นหน้าตั้งค่า |
| 7 | **สถานะระบบกระจาย** — FFmpeg อยู่ sidebar, สุขภาพสตรีมอยู่หน้าตั้งค่า, สถานะไลฟ์อยู่ขั้น 6 | ตอบไม่ได้ทันทีว่า "ตอนนี้ทุกอย่างโอเคไหม" |
| 8 | **หลายบัญชีใช้ chip เล็ก ๆ แถวเดียว** | ไลฟ์ 2–3 บัญชีพร้อมกันแล้วดูภาพรวมไม่ออก |

---

## 6. Flow ใหม่ที่เสนอ (To-be)

### 6.1 หลักการ 5 ข้อ

1. **แยก "ก่อนไลฟ์" ออกจาก "ตอนไลฟ์" อย่างเด็ดขาด** — คนละหน้า คนละอารมณ์
2. **จำเป็น 3 ขั้น / ที่เหลือเป็นของเสริมที่กดเข้าไปทำเมื่อไหร่ก็ได้**
3. **ปุ่มที่กดไม่ได้ ห้ามโชว์ในบริบทที่กดไม่ได้** — ย้ายไปอยู่ที่ที่มันทำงานจริง
4. **ทุกอย่างเซฟเป็น Preset ได้** — ไลฟ์รอบที่ 2 ควรเหลือกดปุ่มเดียว
5. **สถานะรวมอยู่ที่เดียว** — แถบสุขภาพบนหัวจอ ตอบได้ทันทีว่าพร้อมไลฟ์ไหม

### 6.2 ลำดับใหม่ — 3 ขั้น

```
┌──────────────────────────────────────────────────────────────────────┐
│  ขั้น 1 · เตรียมของ            ขั้น 2 · แต่งหน้าจอ      ขั้น 3 · ยิงไลฟ์  │
│  (จำเป็น)                      (ข้ามได้)                (จำเป็น)         │
├──────────────────────────────────────────────────────────────────────┤
│  • เชื่อมบัญชี TikTok           • Overlay รูป/นาฬิกา/    • เช็กลิสต์      │
│  • เลือกคลิป + ลำดับ/สุ่ม        ข้อความ                 • ชื่อ LIVE      │
│                                • ปรับกล้อง              • ปุ่มเริ่มไลฟ์  │
│  [ ข้ามไปยิงไลฟ์เลย → ]        [ ข้าม → ]                              │
└──────────────────────────────────────────────────────────────────────┘
                                    ↓ กด "เริ่มไลฟ์"
┌──────────────────────────────────────────────────────────────────────┐
│                        LIVE CONTROL (หน้าใหม่)                        │
│  แถบสถานะ · พรีวิว · แชทสด · ปักสินค้า · ยอดสด · หยุดไลฟ์             │
└──────────────────────────────────────────────────────────────────────┘
```

**ของที่ย้ายออกจาก wizard:**

| ของเดิม (อยู่ขั้น 4) | ย้ายไปไหน | เหตุผล |
| --- | --- | --- |
| ปลายทางสตรีม (TikTok อัตโนมัติ) | **ซ่อน** — เป็นค่า default | เกือบทุกคนใช้อัตโนมัติ ไม่ต้องเลือก |
| Manual RTMP + Stream key | ใต้ "ตั้งค่าขั้นสูง" ในขั้น 3 | เป็น fallback ไม่ใช่ทางหลัก |
| bitrate + ตัวเข้ารหัส | **อัตโนมัติจากผลเบนช์มาร์ก** + ปรับได้ใน "ขั้นสูง" | ผู้ใช้ไม่รู้ว่าควรตั้งเท่าไหร่ |
| ปักสินค้าอัตโนมัติ | **Live Control** (แท็บ "สินค้า") | ปุ่มปักใช้ได้ตอนไลฟ์เท่านั้น |
| แชทสด & AI | **Live Control** (แท็บ "แชท") + ตั้งค่าเก็บใน Preset | เห็นผลจริงตอนไลฟ์ |
| LINE แจ้งเตือน | ตั้งค่า (เดิม) — ถูกต้องแล้ว | ตั้งครั้งเดียวใช้ตลอด |

### 6.3 Flow ไลฟ์รอบที่ 2 เป็นต้นไป (เป้าหมายสำคัญ)

```
เปิดแอป → หน้าแรก
  └→ การ์ด "ไลฟ์ซ้ำแบบเดิม · ร้านหลัก" → [ เริ่มไลฟ์ ]   ← กดปุ่มเดียวจบ
```

---

## 7. โครงหน้าจอใหม่ (Information Architecture)

```
Rerun Studio
├── 🏠 หน้าแรก            ← เดิม "แดชบอร์ด" แต่เอา "ไลฟ์ซ้ำ" มาไว้บนสุด
├── 🔴 ไลฟ์               ← รวม Wizard 3 ขั้น + Live Control ไว้ที่เดียว
│     ├─ ตั้งค่า (ก่อนไลฟ์)
│     └─ ควบคุม (ตอนไลฟ์)
├── 🎬 คลัง               ← คลิป + Preset overlay + Preset สินค้า/แชท
├── 📊 ผลงาน              ← รวม "สรุปไลฟ์ย้อนหลัง" + กราฟยอดขาย + นำเข้า CSV
└── ⚙️ ตั้งค่า             ← ระบบ · LINE · อัปเดต · เครื่องมือช่าง · แอดมิน
```

**เปลี่ยนอะไรบ้าง**
- ยุบ "แดชบอร์ด" กับ "สรุปไลฟ์ย้อนหลัง" ที่ทับซ้อนกัน → ให้ *หน้าแรก* เป็นจุดเริ่ม, *ผลงาน* เป็นจุดวิเคราะห์
- "โหมดแอดมิน" ย้ายไปเป็นแท็บใน *ตั้งค่า* (ผู้ใช้ทั่วไปไม่เห็นเลยจนกว่าจะปลดล็อก)
- "คลังวิดีโอ" ขยายเป็น "คลัง" เก็บ Preset ได้ด้วย

---

## 8. Wireframe หน้าจอใหม่

### 8.1 หน้าแรก

```
┌────────────┬──────────────────────────────────────────────────────────┐
│            │  ● พร้อมไลฟ์   FFmpeg ✓ · เน็ต ✓ · เครื่องไหว 2 บัญชี      │
│  🏠 หน้าแรก │──────────────────────────────────────────────────────────│
│  🔴 ไลฟ์    │  สวัสดี, คุณเอ                                            │
│  🎬 คลัง    │                                                          │
│  📊 ผลงาน   │  ┌──────────────────────────┐ ┌────────────────────────┐│
│  ⚙️ ตั้งค่า  │  │ ไลฟ์ซ้ำแบบเดิม            │ │ + ตั้งค่าไลฟ์ใหม่      ││
│            │  │ ร้านหลัก · 3 คลิป · 5 layer│ │                        ││
│  ─────────  │  │ ล่าสุด 2 ชม.ที่แล้ว        │ │  เริ่มจากศูนย์          ││
│  ● ร้านหลัก │  │  [ ▶ เริ่มไลฟ์ ]           │ │  [ เริ่มตั้งค่า ]       ││
│  ○ ร้านสอง  │  └──────────────────────────┘ └────────────────────────┘│
│  + เพิ่ม    │                                                          │
│            │  ┌──── สัปดาห์นี้ ────────────────────────────────────┐  │
│  ─────────  │  │  ไลฟ์ 12 ครั้ง · 34 ชม. · ยอดขาย 128,400 บาท      │  │
│  ออกจากระบบ │  │  [กราฟแท่ง 7 วัน]                    ดูทั้งหมด →   │  │
│  v0.7.6    │  └──────────────────────────────────────────────────┘  │
└────────────┴──────────────────────────────────────────────────────────┘
```

**เปลี่ยนสำคัญ:** ปุ่มแรกที่ตาเห็นคือ *ไลฟ์ซ้ำแบบเดิม* ไม่ใช่ *เริ่มไลฟ์ใหม่* — เพราะการใช้งานส่วนใหญ่คือทำซ้ำของเดิม

### 8.2 ไลฟ์ — ขั้น 1 "เตรียมของ"

```
┌──────────────────────────────────────────────────────────────────────┐
│  ●━━━━━━━━━━○────────────○      ร้านหลัก ▾                            │
│  เตรียมของ  แต่งหน้าจอ  ยิงไลฟ์                                       │
├──────────────────────────┬───────────────────────────────────────────┤
│                          │  1  บัญชี TikTok                          │
│      [ พรีวิว 9:16 ]     │     ✓ mystore.official  เชื่อมแล้ว        │
│                          │     [ เปิด TikTok ]  [ เปลี่ยนบัญชี ]      │
│      คลิปแรกที่จะเล่น     │                                           │
│                          │  2  คลิปที่จะวน                            │
│                          │     ┌──────────────────────────────────┐  │
│                          │     │ ⠿ 1. promo-a.mp4      1:20  ▲▼🗑│  │
│                          │     │ ⠿ 2. promo-b.mp4      0:45  ▲▼🗑│  │
│                          │     └──────────────────────────────────┘  │
│                          │     [ + เพิ่มคลิป ]  [ จากคลัง ]           │
│                          │     ◉ สุ่มลำดับทุกรอบ  (กันโดนตรวจซ้ำ)     │
│                          │                                           │
│                          │              [ ข้ามไปยิงไลฟ์ ]  [ ถัดไป → ]│
└──────────────────────────┴───────────────────────────────────────────┘
```

- คิวคลิปเป็น **drag-and-drop** (ยังคงปุ่ม ▲▼ ไว้เผื่อเมาส์ไม่ถนัด)
- ปุ่ม **ข้ามไปยิงไลฟ์** ทำให้คนรีบไลฟ์ได้ใน 2 คลิก

### 8.3 ไลฟ์ — ขั้น 2 "แต่งหน้าจอ" (ข้ามได้)

```
┌──────────────────────────────────────────────────────────────────────┐
│  ●━━━━━━━━━━●━━━━━━━━━━○                                             │
├──────────────────────────┬───────────────────────────────────────────┤
│                          │  [ + รูป ] [ + นาฬิกา ] [ + ข้อความ ]      │
│   ┌──────────────────┐   │                                           │
│   │ ╭──────────────╮ │   │  Layer                                    │
│   │ │  โลโก้ร้าน    │ │   │  ┌─────────────────────────────────────┐ │
│   │ ╰──────────────╯ │   │  │ 🖼 โลโก้ร้าน            👁 ▲▼ 🗑    │ │
│   │                  │   │  │ 🕐 นาฬิกา 24 ชม.        👁 ▲▼ 🗑    │ │
│   │  ลากวางได้เลย     │   │  │ 🅣 "ส่งฟรี 2 ชิ้น"      👁 ▲▼ 🗑    │ │
│   │                  │   │  └─────────────────────────────────────┘ │
│   │  ╭────────────╮  │   │                                           │
│   │  │ส่งฟรี 2 ชิ้น│  │   │  ── เลือก layer แล้วปรับตรงนี้ ──          │
│   │  ╰────────────╯  │   │  ขนาด   ▬▬▬▬▬●▬▬▬                         │
│   │      12:45       │   │  ความจาง ▬▬▬▬▬▬▬●▬                        │
│   └──────────────────┘   │  เอฟเฟกต์ [ ไม่มี ▾ ]                     │
│   ☑ เส้นพื้นที่ปลอดภัย    │                                           │
│                          │  ▸ ปรับกล้อง (ซูม · เลื่อน · พลิก)         │
│                          │              [ ข้าม ]  [ ถัดไป → ]         │
└──────────────────────────┴───────────────────────────────────────────┘
```

**เปลี่ยนสำคัญ:** เดิมทุก layer กางคอนโทรลพร้อมกันหมด → ใหม่เป็น **รายการ layer + แผงคุณสมบัติของตัวที่เลือก** (แบบ Canva/Figma) ลดของบนจอลงกว่าครึ่ง

### 8.4 ไลฟ์ — ขั้น 3 "ยิงไลฟ์"

```
┌──────────────────────────────────────────────────────────────────────┐
│  ●━━━━━━━━━━●━━━━━━━━━━●                                             │
├──────────────────────────────────────────────────────────────────────┤
│  ชื่อ LIVE  [ ไลฟ์สินค้ารอบเย็น                              ]         │
│                                                                      │
│  ┌── ตรวจก่อนยิง ──────────────────────────────────────────────────┐ │
│  │ ✓ บัญชี      mystore.official                                  │ │
│  │ ✓ คลิป       3 คลิป · สุ่มลำดับ                                 │ │
│  │ ✓ Overlay    5 layer                                           │ │
│  │ ✓ ปลายทาง    TikTok อัตโนมัติ                                   │ │
│  │ ✓ คุณภาพ     6 Mbps · ฮาร์ดแวร์ NVENC   (ปรับให้อัตโนมัติ)       │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ▸ ตั้งค่าขั้นสูง (Manual RTMP · bitrate · ตัวเข้ารหัส)               │
│  ▸ เตรียมปักสินค้า / ตอบแชทไว้ล่วงหน้า  (ทำตอนไลฟ์ก็ได้)             │
│                                                                      │
│         [ ← ย้อนกลับ ]        [ ▶  เริ่มไลฟ์ตอนนี้  ]                 │
│         ห้อง LIVE จริงจะถูกสร้างเมื่อกดปุ่มนี้เท่านั้น                  │
│         ☐ บันทึกเป็น Preset "รอบเย็น" ไว้ใช้ซ้ำ                       │
└──────────────────────────────────────────────────────────────────────┘
```

### 8.5 LIVE CONTROL — หน้าใหม่ (แทนขั้น 6)

```
┌──────────────────────────────────────────────────────────────────────┐
│ 🔴 กำลังไลฟ์ 01:24:07 · ร้านหลัก      6.0 Mbps · 30 fps · ปกติ ✓     │
│                                        [ อัปเดตให้ไลฟ์นี้ ] [ ⏹ หยุด ]│
├────────────────────────┬─────────────────────────────────────────────┤
│  ┌──────────────────┐  │  [ แชท ] [ สินค้า ] [ ยอดขาย ]              │
│  │                  │  │─────────────────────────────────────────────│
│  │   พรีวิวหยุดไว้    │  │  ⦿ ตอบอัตโนมัติ เปิดอยู่                    │
│  │   ระหว่างไลฟ์      │  │  ┌───────────────────────────────────────┐│
│  │                  │  │  │ 👤 nook_x  "ตัวนี้เท่าไหร่"            ││
│  │   (ดูภาพจริง     │  │  │ 🤖 ตอบ    "ชิ้นละ 199 บาทค่ะ"  0.4 วิ  ││
│  │    ที่ TikTok)    │  │  │ 👤 mai      "ส่งฟรีไหม"                ││
│  │                  │  │  │ 🤖 ตอบ    "ซื้อครบ 2 ชิ้นส่งฟรีค่ะ"     ││
│  └──────────────────┘  │  └───────────────────────────────────────┘│
│                        │  [ + เพิ่มกฎด่วน ]        [ ปิดตอบอัตโนมัติ ]│
│  กำลังเล่น              │─────────────────────────────────────────────│
│  promo-b.mp4  0:22/0:45│  แท็บ "สินค้า":                              │
│  รอบที่ 7 · สุ่มลำดับ    │   ⦿ ปักอัตโนมัติทุก 3 นาที                   │
│                        │   1. ครีมบำรุงผิว   ●กำลังปัก  [ ปักเลย ]    │
│  ── สุขภาพ ──          │   2. เซรั่ม         ○รอคิว     [ ปักเลย ]    │
│  CPU ▬▬▬▬▬▬░░ 62%      │   3. โฟมล้างหน้า    ⊘ข้ามไว้    [ ใส่กลับ ]  │
│  เข้ารหัส  ทัน ✓        │   🎟 คูปอง         [ ปักคูปองเลย ]           │
└────────────────────────┴─────────────────────────────────────────────┘
```

**เปลี่ยนสำคัญ**
- ปุ่ม **ปักเลย / ปักคูปองเลย** อยู่เฉพาะที่นี่ → กดได้จริงเสมอ ไม่มีปุ่ม disabled ให้งง
- แชท + สินค้า + ยอดขาย เป็น **แท็บเดียวกัน** ไม่ต้องเลื่อนหา
- ปุ่มอันตราย (หยุดไลฟ์) อยู่มุมขวาบน ห่างจากปุ่มที่กดบ่อย

### 8.6 ผลงาน (รวมประวัติ + ยอดขาย)

```
┌──────────────────────────────────────────────────────────────────────┐
│  ผลงาน            [ 7 วัน ][ 30 วัน ][ ทั้งหมด ]   [ นำเข้ายอดขาย CSV ]│
├──────────────────────────────────────────────────────────────────────┤
│  ไลฟ์ 12 ครั้ง  │  34 ชม. 12 น.  │  จบปกติ 11  │  ยอดขาย 128,400 ฿  │
│  ────────────────────────────────────────────────────────────────    │
│  [ กราฟ ยอดขาย + ชั่วโมงไลฟ์ 14 วัน ]                                │
│  ────────────────────────────────────────────────────────────────    │
│  ประวัติไลฟ์                                              ล้างประวัติ │
│  ● 1 ก.ย. 18:02  ร้านหลัก  2:14 ชม.  จบปกติ    12,400 ฿             │
│  ● 1 ก.ย. 10:30  ร้านสอง   1:02 ชม.  ผิดพลาด   —                    │
└──────────────────────────────────────────────────────────────────────┘
```

### 8.7 ตั้งค่า (แท็บ)

```
[ ระบบ ]  [ แจ้งเตือน LINE ]  [ เครื่องมือช่าง ]  [ แอดมิน 🔒 ]

ระบบ           : เวอร์ชัน · อัปเดตอัตโนมัติ · ตรวจหาอัปเดต · FFmpeg · ตัวช่วยฮาร์ดแวร์
                 "เครื่องนี้ไลฟ์ไหวแค่ไหน" → [ ทดสอบเครื่อง ] → รองรับ N บัญชี
แจ้งเตือน LINE  : token · ปลายทาง · เริ่ม/หยุด/ผิดพลาด · [ บันทึก ][ ทดสอบ ][ ส่งสถานะตอนนี้ ]
เครื่องมือช่าง  : สแกนหน้า LIVE console · คัดลอกผล   (ซ่อนใต้ "สำหรับผู้พัฒนา")
แอดมิน 🔒      : ADMIN TOKEN → License Key (ออก / ค้นหา / ถอนสิทธิ์)
```

### 8.8 AdminWEB — ปรับเล็ก

```
┌──────────┬───────────────────────────────────────────────────────────┐
│ ภาพรวม 24│  ภาพรวมระบบ            [ ค้นหา… ]   ☾  เปิดหน้าแอป  ออก   │
│ ลูกค้า 18│───────────────────────────────────────────────────────────│
│ แพ็กเกจ 4│  ลูกค้า 18 │ ใช้งาน 15 │ ใกล้หมดอายุ 3 │ ช่อง 240 │ ฿48,000│
│ ประกาศ 2 │                                                           │
│          │  ⚠ 3 รายชื่อใกล้หมดอายุใน 7 วัน       [ ดู / ต่ออายุ ]      │
│          │  [ แพ็กเกจขายดี ] [ ผู้ใช้ใหม่ 7 วัน ] [ สถานะลูกค้า ]      │
└──────────┴───────────────────────────────────────────────────────────┘
```

**ที่ควรแก้ใน AdminWEB**
1. ฟอร์ม "เพิ่มลูกค้า" กางค้างอยู่เหนือตารางตลอด → เปลี่ยนเป็นปุ่ม **+ เพิ่มลูกค้า** เปิด modal (เหมือนที่หน้าแก้ไขทำอยู่แล้ว)
2. คอลัมน์ "ช่อง" ที่ขึ้น 0 เสมอ → ใส่ tooltip *"นับจากเครื่องลูกค้า ยังไม่ซิงก์"* หรือซ่อนไปเลย
3. เพิ่มปุ่ม **ต่ออายุเร็ว (+30 วัน)** ในแถวลูกค้า — เป็นงานที่แอดมินทำบ่อยที่สุด
4. เพิ่ม **คัดลอกชุดล็อกอิน** (รหัสลูกค้า + username + password) เป็นข้อความเดียวส่งลูกค้า

---

## 9. Design System ที่เสนอ

### 9.1 โทน
แอปนี้เป็น **เครื่องมือทำงานที่ต้องเปิดค้างหลายชั่วโมง** ผู้ใช้คือแม่ค้าไลฟ์ ไม่ใช่สาย IT
โทนที่เหมาะ: **สว่าง สะอาด ตัวหนังสือใหญ่ ปุ่มใหญ่ คอนทราสต์สูง** ไม่ใช่ dashboard สาย dev
(ปัจจุบัน `<meta name="color-scheme" content="light">` — คงธีมสว่างเป็นหลัก แต่เพิ่มโหมดมืดสำหรับไลฟ์กลางคืนได้)

### 9.2 สี

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--bg` | `#F7F8FA` | พื้นหลังแอป |
| `--surface` | `#FFFFFF` | การ์ด / แผง |
| `--border` | `#E4E7EC` | เส้นขอบ |
| `--text` | `#101828` | ตัวหนังสือหลัก |
| `--text-muted` | `#667085` | คำอธิบาย |
| `--primary` | `#FF2C55` | ปุ่มหลัก / แบรนด์ (โทน TikTok) |
| `--live` | `#E5252A` | สถานะกำลังไลฟ์ (จุดกระพริบ) |
| `--ok` | `#12B76A` | เชื่อมแล้ว · จบปกติ · ระบบพร้อม |
| `--warn` | `#F79009` | ใกล้หมดอายุ · bitrate ตก |
| `--danger` | `#D92D20` | หยุดไลฟ์ · ลบ · ถอนสิทธิ์ |
| `--info` | `#2E90FA` | ประกาศ · ทิป |

**กฎสี:** ในหนึ่งหน้าจอมีปุ่มสี `--primary` ได้ **ปุ่มเดียว** เท่านั้น (คือ "สิ่งที่ควรทำต่อ")

### 9.3 ตัวอักษร

| ระดับ | ขนาด | น้ำหนัก | ใช้กับ |
| --- | --- | --- | --- |
| Display | 28px | 700 | หัวหน้าแรก |
| H1 | 22px | 700 | หัวหน้า |
| H2 | 18px | 600 | หัวการ์ด |
| Body | 15px | 400 | ตัวหนังสือทั่วไป (**ห้ามต่ำกว่า 14px**) |
| Caption | 13px | 400 | คำอธิบายใต้ช่องกรอก |
| Mono/Metric | 16–32px | 600 | ตัวเลข bitrate / เวลาไลฟ์ (ใช้ tabular-nums) |

ฟอนต์: `IBM Plex Sans Thai` หรือ `Noto Sans Thai` (ตัวเลขใช้ tabular)
ฟอนต์ตกแต่ง (Audiowide / DSEG / PressStart / VT323) **ใช้เฉพาะใน overlay ที่ออกอากาศ** ห้ามใช้กับ UI

### 9.4 Spacing / Radius

- Grid 4px · ระยะระหว่างการ์ด 16px · padding การ์ด 20px
- Radius: ปุ่ม 10px · การ์ด 14px · pill 999px
- เงา: การ์ดใช้ `0 1px 2px rgba(16,24,40,.06)` เท่านั้น ไม่ต้องเงาหนัก

### 9.5 ขนาดพื้นที่กด (สำคัญ — ผู้ใช้กดพลาดบ่อย)

| ปุ่ม | ความสูงขั้นต่ำ |
| --- | --- |
| ปุ่มหลัก (เริ่มไลฟ์ / หยุดไลฟ์) | 52px |
| ปุ่มทั่วไป | 40px |
| ปุ่มไอคอน (▲▼🗑) | 36 × 36px |
| แถวรายการที่กดได้ | 48px |

### 9.6 กฎ Interaction

1. **ปุ่มทำลายต้องยืนยันเสมอ** — หยุดไลฟ์ / ลบคลิป / ล้างประวัติ / ถอนสิทธิ์ / ลบลูกค้า
2. **ปุ่มที่ทำงานกับของนอกแอปต้องมีสถานะ loading ในตัวปุ่ม** (เข้าสู่ระบบ TikTok / ดึงสินค้า / ปักเลย / ทดสอบ LINE)
3. **ห้าม disabled เฉย ๆ** — ถ้ากดไม่ได้ ต้องมีข้อความบอกว่าเพราะอะไรและต้องทำอะไรก่อน
4. **Toggle เปลี่ยนค่าทันที ไม่ต้องกดบันทึก** — เว้นแต่ฟอร์มที่มีหลายช่อง (LINE)
5. **Toast บอกผลทุกครั้ง** — "บันทึกการตั้งค่าแล้ว" / "ปักสำเร็จ" / "ปักไม่สำเร็จ (เหตุผล)"

---

## 10. ตาราง Map ปุ่มเก่า → ที่อยู่ใหม่

| ปุ่มเดิม | ที่อยู่เดิม | ที่อยู่ใหม่ | หมายเหตุ |
| --- | --- | --- | --- |
| เข้าสู่ระบบด้วย TikTok | ขั้น 1 | ขั้น 1 | เหมือนเดิม |
| เปิด TikTok / ออกจากระบบ TikTok | ขั้น 1 | ขั้น 1 (เมนู ⋯) | เก็บใต้เมนูรอง |
| เลือกวิดีโอ / เพิ่มคลิป | ขั้น 2 | ขั้น 1 | ยุบรวมกับบัญชี |
| ▲▼🗑 คลิป | ขั้น 2 | ขั้น 1 | + drag-and-drop |
| สุ่มลำดับเล่น | ขั้น 2 | ขั้น 1 | เหมือนเดิม |
| เพิ่มรูป/นาฬิกา/ข้อความ | ขั้น 3 | ขั้น 2 | เหมือนเดิม |
| คอนโทรลราย layer | ขั้น 3 (กางหมด) | ขั้น 2 (แผงคุณสมบัติ) | **ลดของบนจอครึ่งหนึ่ง** |
| ปรับกล้อง | ขั้น 3 | ขั้น 2 (พับไว้) | |
| TikTok อัตโนมัติ / Manual RTMP | ขั้น 4 | ขั้น 3 → ตั้งค่าขั้นสูง | default = อัตโนมัติ |
| Server URL / Stream key / 👁 | ขั้น 4 | ขั้น 3 → ตั้งค่าขั้นสูง | |
| bitrate / ตัวเข้ารหัส | ขั้น 4 | ขั้น 3 → ตั้งค่าขั้นสูง | ตั้งอัตโนมัติจากเบนช์มาร์ก |
| เปิด LIVE Manager | ขั้น 4 | Live Control (แถบบน) | |
| ชื่อ LIVE | ขั้น 4 | ขั้น 3 (บนสุด) | |
| Toggle ปักอัตโนมัติ · ทุกกี่นาที | ขั้น 4 | **Live Control → สินค้า** | |
| ดึงสินค้าจากไลฟ์ | ขั้น 4 | **Live Control → สินค้า** | |
| ปักเลย / ปักคูปองเลย | ขั้น 4 (disabled) | **Live Control → สินค้า** | แก้ปัญหาปุ่มกดไม่ได้ |
| chip ข้าม/ใส่กลับสินค้า · ▲▼🗑 | ขั้น 4 | **Live Control → สินค้า** | |
| Toggle ตอบแชทอัตโนมัติ | ขั้น 4 | **Live Control → แชท** (+ Preset) | |
| เพิ่มกฎ / ลบกฎ / Toggle กฎ | ขั้น 4 | **Live Control → แชท** | เพิ่ม "เพิ่มกฎด่วน" จากคอมเมนต์จริง |
| ตั้งค่า AI (provider/key/model/บริบท) | ขั้น 4 | ตั้งค่า → แชท AI (ตั้งครั้งเดียว) | ไม่ควรอยู่ใน flow ไลฟ์ |
| เริ่มไลฟ์ตอนนี้ | ขั้น 5 | ขั้น 3 | เหมือนเดิม |
| หยุดไลฟ์ / อัปเดตให้ไลฟ์นี้ | ขั้น 6 | Live Control (แถบบน) | |
| เริ่มตั้งค่าใหม่ | แถบบัญชี | เมนู ⋯ ของบัญชี | ลดการกดพลาด |
| นำเข้ายอดขาย CSV | แดชบอร์ด | ผลงาน | ไปอยู่กับข้อมูลที่เกี่ยวข้อง |
| ล้างประวัติ | ประวัติ | ผลงาน (เมนู ⋯) | ปุ่มทำลาย ไม่ควรอยู่โล่ง |
| สแกนหน้า LIVE console | ตั้งค่า → ระบบ | ตั้งค่า → เครื่องมือช่าง | ซ่อนจากผู้ใช้ทั่วไป |
| โหมดแอดมิน | Sidebar | ตั้งค่า → แอดมิน 🔒 | ผู้ใช้ทั่วไปไม่ต้องเห็น |

**ผลลัพธ์:** คอนโทรลที่เห็นตอนตั้งค่าไลฟ์ครั้งแรก ลดจาก **~65 → ~28**

---

## 11. Component ที่ต้องออกแบบ

| Component | รายละเอียด | สถานะที่ต้องมี |
| --- | --- | --- |
| `AppShell` | Sidebar + Topbar สถานะรวม | ปกติ / มีอัปเดต / ไลฟ์อยู่ |
| `HealthBar` | แถบบนสุด: FFmpeg · เน็ต · เครื่องไหวกี่บัญชี | พร้อม / เตือน / ไม่พร้อม |
| `AccountSwitcher` | รายการบัญชีใน sidebar + จุดสถานะ | ว่าง / เชื่อมแล้ว / กำลังไลฟ์ / ผิดพลาด |
| `ResumeLiveCard` | การ์ด "ไลฟ์ซ้ำแบบเดิม" | มี preset / ยังไม่มี |
| `StepperNav` | 3 ขั้น + ข้ามได้ | ยังไม่ถึง / กำลังทำ / เสร็จ |
| `PreviewCanvas` | พรีวิว 9:16 ลากวาง layer + เส้น safe area | ว่าง / มีคลิป / หยุดตอนไลฟ์ |
| `ClipQueue` | drag-and-drop + ระยะเวลา + สุ่ม | ว่าง / 1 คลิป / หลายคลิป / สเปคไม่ตรง |
| `LayerList` + `LayerInspector` | รายการ layer + แผงคุณสมบัติ | ไม่ได้เลือก / เลือกรูป / นาฬิกา / ข้อความ |
| `PreflightChecklist` | เช็กลิสต์ 5 บรรทัดก่อนไลฟ์ | ผ่าน / ติด (บอกวิธีแก้) |
| `LiveHeaderBar` | เวลาไลฟ์ · bitrate · fps · ปุ่มหยุด | ปกติ / เข้ารหัสไม่ทัน / หลุด |
| `ChatStream` | คอมเมนต์เข้า + คำตอบที่ส่ง | รอคอมเมนต์ / มีข้อความ / AI ตอบไม่สำเร็จ |
| `PinQueue` | ลำดับสินค้า + สถานะปัก + ปุ่มปักเลย | ยังไม่มีสินค้า / กำลังปัก / ข้ามไว้ |
| `MetricTile` | ตัวเลขสรุป | มีข้อมูล / ยังไม่มีข้อมูล |
| `Toast` | ผลลัพธ์การกระทำ | สำเร็จ / ผิดพลาด (มีเหตุผล) |
| `ConfirmDialog` | ยืนยันการทำลาย | — |
| `AdminTable` (AdminWEB) | ตาราง + action ต่อแถว + ต่ออายุเร็ว | ว่าง / มีข้อมูล / กำลังโหลด |

---

## 12. Empty / Loading / Error State ที่มีอยู่จริง (ใช้ต่อได้เลย)

| ที่ | ข้อความ |
| --- | --- |
| ยังไม่มี overlay | *ยังไม่มี overlay — เพิ่มรูป นาฬิกา หรือข้อความจากปุ่มด้านบน* |
| ยังไม่มีกฎแชท | *ยังไม่มีกฎ — เพิ่มคีย์เวิร์ดที่พบบ่อย เช่น "ราคา"* |
| ยังไม่มีสินค้า | *ยังไม่มีสินค้า — กดไลฟ์แล้วกด "ดึงสินค้าจากไลฟ์"* |
| ยังไม่มีประวัติ | *ยังไม่มีประวัติไลฟ์ — เริ่มไลฟ์แรกของคุณได้เลย* |
| คลังว่าง | *ยังไม่มีวิดีโอในคลัง* |
| กำลังเริ่ม | *กำลังเริ่ม pipeline…* · *ครั้งแรกจะใช้เวลาเตรียมสักครู่ (ทำครั้งเดียว)* |
| เครื่องไม่ไหว | *เครื่องเข้ารหัสไม่ทัน — ภาพจะกระตุก ลองลด bitrate หรือปิดโปรแกรมอื่น* |
| ปักไม่ได้ | *ใช้ได้เฉพาะตอนกำลังไลฟ์ เพราะปุ่ม Pin จะมีในหน้า TikTok ต่อเมื่อไลฟ์แล้ว* |
| ไม่มีคูปอง | *ไม่พบคูปองในไลฟ์นี้ — ต้องสร้างคูปองในหน้า TikTok ก่อน* |
| พรีวิวตอนไลฟ์ | *พรีวิวหยุดไว้ระหว่างไลฟ์เพื่อลดภาระเครื่อง — ดูภาพจริงได้ที่ TikTok* |
| ความปลอดภัย | *ระบบเก็บ browser session ไว้เฉพาะเครื่องนี้ และไม่อ่านหรือส่งออกรหัสผ่านของคุณ* |

> เก็บน้ำเสียงนี้ไว้ทั้งหมด — เป็นจุดแข็งของแอปเดิม (พูดเหมือนคนคุยกัน อธิบายเหตุผล ไม่ใช่ error code)

---

## 13. Brief สำหรับ Claude Design

**สิ่งที่อยากได้**

1. High-fidelity mockup ของ 6 หน้าจอนี้ (desktop 1440 × 900, Electron ไม่มี browser chrome)
   - หน้าแรก
   - ไลฟ์ ขั้น 1 เตรียมของ
   - ไลฟ์ ขั้น 2 แต่งหน้าจอ
   - ไลฟ์ ขั้น 3 ยิงไลฟ์
   - **Live Control** (หน้าสำคัญที่สุด)
   - ผลงาน
2. หน้าจอเสริม: Login (License) · ตั้งค่า · AdminWEB ภาพรวม + ตารางลูกค้า
3. ชุด Component + Design token ตามข้อ 9 และ 11
4. State ครบ: empty · loading · error · กำลังไลฟ์

**ข้อจำกัดที่เปลี่ยนไม่ได้**

- ภาษาไทยทั้งหมด (เก็บถ้อยคำเดิมตามข้อ 12)
- พรีวิวเป็น 9:16 เสมอ
- Electron desktop — ไม่ต้องทำ responsive มือถือ แต่ต้องไม่พังที่ 1280 × 720
- ฟีเจอร์ทั้งหมดในข้อ 4 ต้องยังเข้าถึงได้ (ย้ายที่ได้ แต่ห้ามหาย)
- ปุ่ม "เริ่มไลฟ์ตอนนี้" ต้องเป็นการกระทำที่ตั้งใจชัดเจน (สร้างห้อง LIVE จริง)

**เกณฑ์วัดผล**

| ข้อ | เป้า |
| --- | --- |
| ไลฟ์ครั้งแรกสำเร็จ | ≤ 3 ขั้น · ≤ 8 คลิก (จากเปิดแอป) |
| ไลฟ์ซ้ำของเดิม | **1 คลิก** จากหน้าแรก |
| คอนโทรลบนจอตอนตั้งค่า | ≤ 30 (จาก ~65) |
| หาปุ่ม "ปักสินค้าเดี๋ยวนี้" ตอนไลฟ์ | ≤ 2 คลิก และกดได้จริง 100% |
| ปุ่ม disabled ที่ไม่บอกเหตุผล | 0 ปุ่ม |

---

## ภาคผนวก — API ภายใน (IPC) ที่ปุ่มแต่ละอันเรียก

ใช้ตรวจว่าปุ่มในดีไซน์ใหม่ยังครอบคลุมความสามารถเดิมครบ (จาก `dist-electron/preload.js`)

| กลุ่ม | ช่องทาง |
| --- | --- |
| License | `license:login` · `license:status` · `license:logout` |
| อัปเดต | `update:get-status` · `update:check` · `update:install` · `update:get-config` · `update:set-config` · event `update:status` |
| ประกาศ | `announcements:get` |
| แอดมินในแอป | `admin:unlock` · `admin:status` · `admin:lock` · `admin:issue-key` · `admin:lookup` · `admin:revoke` · `admin:list` |
| สื่อ | `media:choose-video` · `media:probe-clips` · `media:choose-overlay` |
| TikTok | `tiktok:login` · `tiktok:status` · `tiktok:logout` · `tiktok:open` · `tiktok:open-shop` · `tiktok:scan-live-console` |
| ปักสินค้า | `pin:get-config` · `pin:set-config` · `pin:list-products` · `pin:pin-now` · `pin:coupon` · `pin:live-stats` |
| สตรีม | `stream:start` · `stream:apply-config` · `stream:stop` · `stream:status` · event `stream:status-changed` · `stream:health` |
| แชท | `chat:get-config` · `chat:set-config` · event `chat:event` |
| LINE | `line:get-config` · `line:set-config` · `line:test` · `line:push-status` |
| ประวัติ | `history:list` · `history:clear` · event `history:changed` |
| ยอดขาย | `sales:choose-file` · `sales:preview` · `sales:commit` · `sales:list` · `sales:remove-batch` |
| คลัง | `library:list` · `library:add` · `library:remove` |
| ระบบ | `app:info` · `system:benchmark` · `shell:open-external` |

> ⚠️ ยังไม่มี API สำหรับ **Preset** (บันทึกชุดตั้งค่าไว้ใช้ซ้ำ) — เป็นของใหม่ที่ต้องเพิ่มฝั่ง main process เพื่อรองรับ flow "ไลฟ์ซ้ำ 1 คลิก"
